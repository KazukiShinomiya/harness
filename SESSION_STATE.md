# SESSION_STATE

最終更新: 2026-08-13

## 現在の状況

多層防御が**四層**になった。`tests/run.sh` は 112 項目すべて成功し、**push と PR のたびに
GitHub Actions でも走る**（`.github/workflows/tests.yml`）。
**リポジトリは public**（<https://github.com/KazukiShinomiya/harness>）。master に push 済み。

**この機・この版では `ask` は両経路とも効く。** ネイティブ Linux 6.8 / Ubuntu 24.04.4
（**WSL2 ではない**）、v2.1.226。いずれも単独コマンドで撃ち、人間が確認ダイアログを目視した。

| 版 / 機 | 経路 | 結果 |
|---|---|---|
| v2.1.220 / WSL2（Linux 5.15） | PreToolUse の ask | 素通し |
| **v2.1.226 / この機** | **PreToolUse の ask** | **確認ダイアログが出る** |
| **v2.1.226 / この機** | **`permissions` の ask** | **確認ダイアログが出る**（08-10 に測り直し） |
| 非対話 `-p` | PreToolUse の ask | denied（測定不能） |

**「v2.1.226 で直った」とは書けない。** v2.1.220 の実測は WSL2 機で取ったもので、
版と機が同時に動いている。同じ機で両方の版を撃つまで原因は切り分けられない。

**2026-08-13 に版が v2.1.229 へ上がっているのを確認した（起動画面）。この版では `ask` を
測り直していない。** 上の表はすべて v2.1.226 で取ったもので、現行版の実測ではない。

非対話の denied は**何の証拠にもならない**。`-p` は確認を出す相手がいないので安全側へ
倒れるだけだ（debug ログに `Hook result has permissionBehavior=ask` →
`Bash tool permission denied` と並んで残っている）。

**`permissions.ask` の素通しは 2026-08-10 に撤回した。** 2026-08-09 の記録は下記の
測定ミスと同じ穴を持っていた。`allow` にある `Bash(echo:*)` を `ask` で上書きし、
`echo` を単独で撃ち直したら確認は出た。**allow を上書きする形で測るのが要点**——
`allow` に無いコマンドで測ると、既定の確認と `ask` 由来の確認が区別できない。

### 確認の不在は、エージェントには測れない

**2026-08-10 に一度、これで誤った結論を出した**（「同じ環境でもセッションによって割れる」）。
記述を書き換えてコミットまでした。原因は二つ重なっていた。

1. **複合コマンドで撃った。** `rm -f /tmp/x; echo "rc=$?"` の後半が `allow` に一致して
   全体が自動承認され、`ask` の不発と区別がつかなくなっていた。判定器を `deny` へ
   差し替えるとブロックされたのを「出力は届いているが `ask` だけ落ちる」証拠と読んだが、
   これは `deny` が `allow` に勝つだけのことだった。**決定事項に明記してあった罠に
   自分から落ちた**
2. **確認の不在をエージェントが判定しようとした。** 出て人間が承認した場合と、出ずに
   実行された場合とで、ツールの戻り値が同じになる。実際この日、人間は**無意識に
   `Yes` を押していた**

**設計は変えない。** 確実に止めたいものは `deny` と git 層へ寄せたままにする。
`ask` が効く環境でも、効くことをエージェント側から確認する手段が無い以上、
それに寄りかかった設計にはできない。

**どの層が実際に効いているかは、ここに書かない。** 起動時の自己診断が四層を毎回実測して
報告するので、それを読むこと。手で書いた表は機械が変われば嘘になる——実際、2026-08-02 版の
表は「permissions 12 件・git 層適用済み・OS/FS 層適用済み」と書いていたが、この機では
三層とも入っていなかった。診断を入れた初日にそれが露見した。

## 前回の戦果

**`session-harness` に注入の上限を入れた（`userConfig.max_bytes`、既定 65536）。** それまで
状態ファイルは**何バイトあっても全量が毎セッション文脈へ入っていた**——「軽量に保つ」は
`SKILL.md` が書いている原則でしかなく、破っても何も起きなかった。超えた分は行単位で
末尾から落とし、**残すのは先頭**（次のセッションが要るものは「現在の状況 / 前回の戦果」に
寄っている）。切ったことは `systemMessage` でユーザーの画面にも出す——縮める判断ができるのは
人間だけで、`additionalContext` は人間には見えない。テストは 103 → 112。
**画面に届くことまで実セッションで確かめた**——SESSION_STATE を 136KB へ膨らませて
新セッションを立て、起動画面に警告が出るのを人間が目視した（v2.1.229）。
テスト内の JSON に `systemMessage` があることは、**それが人間に届く証拠にならない**。

**テスト 9 件のうち 1 件は、6 種類の変異では落ちなかった。** 「上限内なら切らない」だけが
どの壊し方でも通り、7 個目（`if dropped:` を `if True:` にする）を作って初めて落ちた。
**「壊した複製で落ちる」は変異を一つ作れば済む話ではない**——旧実装相当の変異（切らない）
だけで測っていたら、「切ったら先頭は残る」（全量注入でも `行 0` は出る）と
「上限内なら切らない」の 2 件が、効かないまま緑で通り続けていた。

**README に撤回漏れが一箇所残っていた。** 「`permissions` の `ask` ルールはどの版でも
素通しになる」——08-10 に撤回したはずの断定が permissions 層の節（`README.md:246`）に
生きていた。詳細節と TODO は直っていたのに、そこだけ古いままだった。**撤回は一箇所を
直して終わりにならない。** 環境依存として書き直した。

`session_state.py` の docstring も列挙と数が合わなくなっていた（「三通り」に 4 つ並ぶ）。

### 08-10 の戦果（要点）

**`protected_paths` の Bash 対応を一段進めた。** `bash -c 'echo x > .env'` のように
サブシェルで包むと素通りしていた——**引数はただの文字列なので静的に読めるのに、
「静的には無理」の側へ誤って分類していた**。同じ判定を再帰的に当てて塞いだ
（`sh` `zsh` `dash` `ksh` の `-c` と `eval` も同様）。テストは 95 → 103。
**壊した複製で落ちることまで確認**——再帰を殺すと 5 件、深さ制限を 10 へ緩めると 1 件が、
狙いどおりに落ちる。書き込み先を取るオプション（`curl -o` 等）は意図的に追わない。

**README に実測と食い違う記述が一つあった。** `exec 3> .env` は「拾えない」と書いて
あったが、実際は発火する（`REDIRECT` が `3>` に一致する）。直した。

**表が埋まりきった。`permissions.ask` も効く。** `allow` にある `Bash(echo:*)` を `ask` で
上書きし、`echo harness-ask-probe` を単独で撃つと確認が出た。人間が目視して No を押した。
2026-08-09 の「素通し」はこれで撤回。**`selfcheck.py` の注意書きが毎セッション嘘を
ついていた**（「permissions の ask ルールはどちらでも表示されない」）ので最優先で直し、
`README.md` と `docs/upstream-report-draft.md` も揃えた。

**`.claude/settings.local.json` の無視がグローバル設定頼みだったのを直した。** 効いて
いたのはこの機の `~/.config/git/ignore` で、機ごとの設定なので他のクローンには無かった。
Claude Code がこのファイルへ書き戻す性質と重なると、権限の実験値が残ったまま `git add` に
拾われうる。リポジトリの `.gitignore` へ移し、`git -c core.excludesFile=/dev/null
check-ignore -v` で測って**グローバルを無効にしても `.gitignore` 側がマッチする**ことを
確認した。

**CI の `actions/checkout` を v5 へ上げた。** v4 は Node 20 を対象にしていて非推奨になり、
ランナー側で Node 24 に強制されて警告が出ていた。Node 24 への移行は v5.0.0 で入ったので
そこまで。最新は v7.0.1 だが ESM 化と fork PR のチェックアウト禁止を含むので採らなかった。
**消えたことは `watch` の見た目ではなく annotation の件数で測った**（1 → 0）。

テストは 103 / 0。この日のコミットはいずれも push 済みで、GitHub Actions も success。

**測り方の道具が一つ増えた。人間が「No を押す」と決めておくと、確認が出たことが
エージェント側にも届く**——拒否はツールがエラーを返すので、承認と戻り値が分かれる。
ただし**人間の目は省けない**。「確認ダイアログの No」と「ESC による割り込み」が
エージェントに届く文言では区別できず、今回どちらだったかは人間に聞いて初めて分かった。

**表の PreToolUse 側の一マスも埋まった**（v2.1.220 では素通りだった経路で確認が出た）。
そのあと測り方を誤って結論が二度ひっくり返り、記述を書き換えてコミットまでした——
経緯と教訓は「確認の不在は、エージェントには測れない」に残してある。表より価値がある。

**確認ダイアログの理由文を書き直した。** 人間から「英語で内容が分からず、何を判断すれば
よいのか分からない」と言われて気付いた。以前の文はエージェントへの戒め
（`NO fabricated consent -- confirm full-context read …`）で、操作の種類によらず同じ。
**止められた人間の判断材料が一つも無かった。** 上の「無意識に Yes」はこれが原因の一部でもある。
いまは操作ごとに「実行すると何が起きるか」を日本語で出す（`CONSEQUENCES` テーブル）。
説明を持たない `extra_patterns` 由来のものは、そう言う。テストを 4 件追加（91 → 95）。

記述は全箇所そろえた（`selfcheck.py` の注意書き / `README.md` に版・機ごとの実測表と
「確認が出なかったはエージェントには測れない」節 / `install.sh` / `dotfiles/.claude/CLAUDE.md`
（正本。symlink なので dotfiles 側を編集）/ `docs/upstream-report-draft.md`）。
**`install.sh` の「起動時の文脈に次の行が出るか見る」は誤りだった**——自己診断は人間の
画面に出ない。案内どおり試して引っかかった。**ただし 08-13 に撤回漏れが一箇所見つかった。**

**テストを 3 件足した**（88 → 91）。`decision=ask` のときに手動確認手順を添えることは
`selfcheck.py` の docstring が挙げる設計要件なのに、未検査のまま残っていた。
壊した複製 2 種で落ちることまで確認済み——caveat を削除すると 2 件、
`if current == "ask"` を無条件化すると 1 件が、狙いどおりに落ちる。

**2026-08-09 の戦果（要点）。** プラグイン層だけが未適用だった（marketplace 未登録）のを
見つけて入れた——**自己診断が拾うはずの故障を、診断を載せている層自体が抜けていたために
誰も報告しなかった**。「状態は書かずに測る」には測る器械が生きている確認が要る。
器械の不在だけは器械が言えない。あわせて dotfiles と役割を整理（正本が 14 件先行しており
双方向マージ。deny 7 件をこの機から正本へ返して push 済み `417dc14`。`~/.claude/hooks/` が
存在せず正本の SessionStart が丸ごと不発だったのも解消）、`install.sh` を隔離 `HOME` で
なぞって「プラグイン層しか入らないが、残り三層は診断が名指しで未適用と言う」ことを確認、
`immutable.sh` の代償記述を実態へ合わせ `status` を測って出す形へ変更。

過去の戦果（要点のみ）: 自己診断を四層へ拡張、判定器の穴を三つ（`rm` の綴り一致→意味判定、
`--build` の誤爆、`protected_paths` の Bash 対応）、CI 導入、この機への git / permissions /
OS/FS 三層の適用。

順番に意味がある。**`Bash(sudo:*)` を deny する前に `trash-cli` を入れること。**
deny は再起動不要で即座に効くため、先に入れるとエージェント経由の `sudo` が塞がる。

## 次の行動

1. **上流 [#79356](https://github.com/anthropics/claude-code/issues/79356) への続報は見送り
   （2026-08-10 判断）。** 撃ち直した結果、この機・この版では両経路とも効く。投稿済み
   コメントは v2.1.220 / WSL2 の実測なので、今回の結果はそれを**否定しない**——版も機も
   違う。「v2.1.226 で直った」と書くには同じ機で両方の版を撃つ必要があり、それはできて
   いない（WSL2 機で v2.1.226 を撃つか、この機に v2.1.220 を入れるか）。**出せる新事実が
   無いので投稿しない。** 交絡が解けたら `docs/upstream-report-draft.md` の続報節が素材。
   **投稿は外向きなので人間の承認を得てから。**
2. **`protected_paths` の穴は残る（意図的な線引き）。** 2026-08-10 にサブシェルの
   再帰解析を入れて一段塞いだ。残るのはインタプリタの中身（`python -c`）、実行時に
   決まる変数展開、書き込み先を取るオプション（`curl -o` / `wget -O` / `gpg --output` /
   `rsync` / `tar -C`）、入れ子 4 段目から先。**追い始めると「次は何を足すのか」が
   永久に残る**ので、広げるより git 層・OS/FS 層へ寄せる方が筋がいい。
   **オプションの無い `rm file` も発火しない**——日常的すぎるのと、戻す役目は
   `rm-guard` が持っているため。変えるなら `find_rm_hit()` の 1 行。
3. `chattr +i` は未適用のまま（2026-08-09 に `status` を実測。三候補とも「通常」）。
   掛けるなら `osfs/immutable.sh status` を読んでから選ぶこと。**現時点では三つとも
   見送りが妥当。** `chattr +i` は「もう動かさないと決めた設定」に掛ける層で、
   直前までいじっていたものに掛けるものではない。
   - **`~/.claude/settings.json` は掛けてはいけない。** dotfiles の `setup.sh` が
     `os.replace()` で置き換える生成物なので、固めると正本の更新がこの機へ反映
     できなくなる。Claude Code 自身も権限追加で書き戻すので二重に詰む
   - `~/.gitconfig` は守る価値が上がった（`core.hooksPath` を入れたので）が、
     `git config --global` の全滅は重く、層の調整がまだ続く。見送り
   **`~/.ssh/authorized_keys` を固める案は見送ったままでよい。ただし理由が機ごとに違う。**
   この機では `openssh-server` は導入済み（1:9.6p1）、`ssh` サービスは **active**、
   `authorized_keys` も**存在する**（0 バイト・鍵ゼロ）。「未導入だから守る対象が無い」
   という以前の記述はこの機には当てはまらない。それでも結論は同じ——空のまま固めると
   自分が鍵を足すときに詰まるだけ。鍵を入れたら、そのとき固めればよい。
4. `session-harness` の注入上限は**実セッションでの目視まで済んだ**（2026-08-13）。
   積み残しは無い。

## 決定事項・メモ

- **状態は書かずに測る。** 手で書いた状態表は書いた瞬間から腐り、機械をまたぐと嘘になる。
  「今どうなっているか」は自己診断に言わせ、ここには「なぜそう決めたか」だけ残す。
- **文書に書いた原則は、破っても何も起きないなら守られない。** SESSION_STATE を「軽量に
  保つ」は `SKILL.md` に書いてあったが、太らせても帰結が無かった（1MB 置けば 1MB 注入された）。
  毎セッション全量が文脈へ入るものは、機械側に上限を持たせる。**止めるのではなく、
  落としたことを人間に伝える**——縮める判断ができるのは人間だけで、
  `additionalContext` は人間の画面に出ない。
- **撤回は一箇所直して終わりにならない。** 08-10 に「`permissions.ask` は素通し」を撤回した
  とき、詳細節と TODO は直したのに permissions 層の節（`README.md:246`）が古い断定のまま
  残っていた（08-13 に発見）。**撤回したら、同じ主張を grep で洗ってから終わること。**
- **未適用は警告しない。故障だけ警告する。** 未適用で毎セッション鳴らすと、やがて誰も
  読まなくなり、本当の故障も一緒に見落とす。
- **状態表を消しても、決定の根拠に混ざった機械固有の事実は残る。** 「`sshd` が動いていない
  から `authorized_keys` は守らなくていい」のような判断は、機械が変われば前提から崩れる。
  結論だけ移して前提を確かめ直さないと、正しい結論を間違った理由で持ち続けることになる。
  次に判断を書くときは「どの機で測ったか」を添えること。
- **`.github/workflows/` を含む push は HTTPS リモートでは通らない。** OAuth トークンに
  `workflow` スコープが無いため GitHub が弾く。**origin は SSH へ切り替え済み**
  （`git@github.com:KazukiShinomiya/harness.git`）なので、以後は素の `git push origin master`
  で通る。他の機や別クローンで同じことに当たったら、そこも SSH にすること。
- **`!` 経由の `sudo` はパスワードを読めない。** TTY が無いため。この機は passwordless
  sudo ではないので、`sudo` が要るものは実端末で打つしかない。`python3-venv` も不完全で、
  sudo 抜きに pip で入れる逃げ道も無かった。
- **黙らないことと止めることは別。** 危険でないものを止める必要は無いが、黙ってよいわけでもない。
  `session-harness` は失敗も不在も報告するが、止めはしない。
- **自己診断は人間の画面には出ない。** `additionalContext` は Claude にだけ渡り、正常時は
  `systemMessage` を出さない設計なので、起動画面は無言になる。人間が四層の生死を知るには
  Claude に尋ねるしかない（「ツールを実行せず、入っている診断をそのまま貼って」。
  実行させると「フック経由で読まれたか」ではなく「スクリプトが動くか」を測ることになる）。
  `install.sh` に「起動時の文脈に次の行が出るか見る」と書いてあったのは誤りで、
  2026-08-10 にその案内どおり試して引っかかった。修正済み。
  **ただし `systemMessage` を出せば人間の画面に出る（2026-08-13 に実測、v2.1.229）。**
  SESSION_STATE を 136KB へ膨らませて新セッションを立てると、起動画面に
  `SessionStart:startup says: session-harness: … 上限 65536 バイトを超えた …` が出た
  （人間が目視して報告）。**「画面に出ない」のは正常時に何も出さない設計だからで、
  出す手段が無いわけではない。** 黙っていてよいものと、人間に届けるべきものを分ける。
- **OS/FS 層だけ思想が違う。止めずに取り消せるようにする。** `rm` は日常的に使うので確認を
  挟むと長続きしない。消えたものが戻せるなら止める必要がない。
- **ゴミ箱が使えないとき `rm-guard` は本物の `rm` へ落とさず止まる。** 落とすと「守られている
  つもりで守られていない」状態が黙って続く。このリポジトリが潰そうとしている失敗そのもの。
- **スクリプトの後始末は `HARNESS_RM_REAL=1 /usr/bin/rm` で書く。** 素の `rm` だと一時ファイルが
  全部ゴミ箱へ溜まり、本当に戻したいものが埋もれる。`tests/run.sh` の `trap` が最初の被害者に
  なった。捨てると分かっているものまでゴミ箱へ送らないこと。
- **fail-safe の既定は `ask` のまま。** `deny` にすると判定器が壊れた瞬間に作業不能になる。
- **permissions は実行時に読み直される。hooks は新セッションでしか読まれない。** 混同しない。
  権限の挙動を試すのに新セッションは要らない（`.claude/settings.local.json` は gitignore 済みで
  実験に使える。ただし Claude Code が書き戻すので実験値は最後に消して `grep -c` で確認する）。
- **確認が出なかったことを、エージェントは観測できない。** 出て人間が承認した場合と、
  出ずに実行された場合とで、ツールの戻り値が同じになる。観測できるのは `deny` による
  ブロック（ツールがエラーを返す）と、人間が画面を見て報告した内容だけ。
  **「素通りした」とエージェントが書いていたら、それは推測である。**
  2026-08-10 にこれで誤った結論を出し、コミットまでした（詳細は現在の状況）。
  **例外は「人間が No を押す」と先に決めておく場合**——拒否はツールがエラーを返すので、
  確認が出たこと自体はエージェントにも届く。ただし**それで人間の目は省けない**。
  「確認ダイアログの No」と「ESC による割り込み」は届く文言が同じで、どちらだったかは
  人間に聞くしかない（2026-08-10 に実測。実際に聞いて初めて分かった）。
- **`permissions.ask` は `allow` を上書きする形で測る。** `allow` に無いコマンドで測ると、
  既定の確認と `ask` 由来の確認が区別できない。`Bash(echo:*)` のように普段 `allow` で
  自動承認されるものを `ask` に置き、単独で撃つ。優先順位は `deny` > `ask` > `allow` で、
  実際に `ask` が `allow` に勝った（2026-08-10、v2.1.226 / この機）。
- **権限の実験は単独コマンドで撃つ。二度目の被害。** 既に決定事項に書いてあったのに、
  `rm -f /tmp/x; echo "rc=$?"` という形で撃って同じ罠に落ちた。`echo` が `allow` に
  拾われて全体が自動承認されていた。**`deny` への差し替えがブロックされたことを
  「フック出力は届いている」の証拠に使ったのも誤り**——`deny` は `allow` に勝つので、
  この対照は素通りの原因を切り分けない。
- **`claude -p` では対話の権限挙動を測れない。** 非対話には確認を出す相手がいないので
  `ask` は denied へ倒れる。「止まった」を「直った」と読み違えないこと。
  `-p` で測れるのは判定器の戻り値とフックの読み込みまでで、その先の見え方は測れない。
- **別セッションを立てたら、その報告ではなく `--debug-file` を読む。** 走らせた側の応答は
  観測結果ではなく解釈が混ざる。実際 `-p` の応答は「経路によって落ち方が違う可能性」と
  述べたが、ログを読むと違いはモード（対話 / 非対話）だった。決定的なのは
  `Hook result has permissionBehavior=ask` と `Bash tool permission denied` の 2 行。
- **権限の実験に複合コマンドを使わない。** `probe --check; echo "rc=$?"` の後半が `allow` に
  マッチしていて、素通りが `ask` の失敗によるものか `allow` の勝ちによるものか切り分けられなく
  なった。`deny` は `allow` に勝つので対照側だけ無傷に見え、危うく誤った結論を出すところだった。
  単独コマンドで撃ち直すこと。終了コードが要るなら別の呼び出しで取る。
- **層ごとに得意な粒度が違う。** `permissions.deny` はコマンド名単位、git 層は引数・内容単位、
  プラグインは動的判定、OS/FS 層は呼び出し元を問わない。同じことを複数層でやろうとしない。
- **判定器は `guard.sh` 経由で呼び、exit 2 を返さない**（ブロック扱いになるため）。
- **導入は手順ゼロにできない。** プロジェクトスコープの `extraKnownMarketplaces` は効かない（実測）。
- **`install.sh` がプラグイン層しか入れないのは、それでよい**（2026-08-09 に隔離 `HOME` で
  実測して決着）。残り三層は自己診断が名指しで「未適用」と報告するので、入れた人は気付ける。
  導入スクリプトを厚くするより、診断が黙らないことに寄りかかる。
- **この機の marketplace はローカルパス登録**（`~/repos/harness`。`install.sh` の GitHub 経由ではない）。
  ここは harness 自体の開発機なので、ローカルの修正が push を挟まず即反映される方が都合がよい。
  外から入れる人は `install.sh` をなぞる。`claude plugin marketplace list` は絶対パスで解決を報告する。
- **dotfiles と harness の役割分担。** 正本 `dotfiles/.claude/settings.json` が
  `enabledPlugins` と `extraKnownMarketplaces` を持ち、harness が実体を配る。
  dotfiles の `setup.sh` は**導入まではせず**、未登録なら手順を出して知らせるだけ
  （外部リポジトリの取得は人間の手に残す設計）。`~/.claude/settings.json` は
  正本＋`settings.machine.json` の**シャロー top-level マージ**を `setup.sh` が生成する
  プレーンコピー——正本を編集したらこの機で `setup.sh` を再実行しないと反映されない。
  マージが浅いので `settings.machine.json` に `permissions` を書くと正本の同ブロックが丸ごと消える。
- **層を settings.json からプラグインへ移す瞬間、谷間ができる。** プラグインのフックは
  新セッションでしか読まれないのに、`settings.json` の撤去は即座に効く。移譲した当日の
  セッションは無防備になる。順序を選べるなら、プラグインを入れてから撤去すること。
- **`githooks` の保護パターンと `protected_paths.py` は意図的に重複させてある。**
  片方を変えたらもう片方も見ること。`tests/run.sh` が主要パターンの両層存在を確認する。
- **変更したら `tests/run.sh` を通すこと。** 使い捨てのコマンドで確認して終わりにしない。
- **CI の警告が消えたかは annotation の件数で測る。** `gh run watch` の出力に出ないことは
  消えた証拠にならない（表示は run の取得タイミングで変わる）。決定的な単一値はこれ:
  `gh api repos/<owner>/<repo>/commits/<sha>/check-runs --jq '[.check_runs[].output.annotations_count] | add'`
  2026-08-10 に `checkout` を v5 へ上げたとき、前後のコミットで 1 → 0 を確認した。
- **テストを足したら、壊した複製で落ちることまで確かめる。** 通ることは効くことの証明に
  ならない。`immutable.sh` に足した 3 件は、旧実装へ戻した複製で走らせたら **2 件が
  そのまま通った**——`contains 'ファイルが無い'` は候補リスト側の状態表示に、
  `contains 'canonical.json'` は `readlink -f` の出力にマッチしていた。
  部分文字列で見るテストは、狙った行以外にマッチしていないかを疑うこと。
  直したあとは複製側で 3 件とも落ちることを確認した。
  **旧実装相当の変異を一つ作るだけでは足りない。** 08-13 に足した 9 件では、変異を 6 種
  当てても「上限内なら切らない」が一度も落ちず、7 個目（`if dropped:` → `if True:`）で
  ようやく落ちた。**テスト一件につき、それだけが落ちる壊し方を一つ用意すること。**
- **「静的には無理」という分類を疑うこと。** `protected_paths` は `bash -c '…'` を
  「シェルは任意の書き込み方を許すから拾えない」側に置いていたが、**`-c` の引数は
  ただの文字列**で、同じ判定を再帰的に当てれば読めた。塞ぐまでは、サブシェルで包む
  だけでこの層を抜けられていた（2026-08-10 に実測）。本当に無理なのは実行時に決まる
  もの（変数展開、インタプリタの中身）だけで、その手前に「読めるのに読んでいない」層が
  あった。**穴を「原理的に無理」と書いた瞬間、誰も測り直さなくなる。**
- **`userConfig` の `multiple: true` はカンマ区切り**（`String(配列)`。生バイトで確認）。
  設定は新セッションでしか読まれず、プロジェクトスコープの `pluginConfigs` は効かない。
- v2.1.220 の validator は `metadata.pluginRoot` による source 短縮形を拒否する。
- **起動は必ず `~/repos/harness` から。** `/mnt/e/work/harness` で起動すると SESSION_STATE が
  読み込まれない（`session-harness` が「どこを探したか」を報告するので気付ける）。
- **Claude Code は `.claude/settings.local.json` をメモリ上の設定で書き戻す。** 実験で入れた
  `pluginConfigs` を消しても、権限追加のタイミングで復活した。実験値は最後に必ず確認して消すこと。
  2026-08-10 にリポジトリの `.gitignore` へも入れた。それまで無視していたのはこの機の
  グローバル `~/.config/git/ignore` だけで、**他のクローンには保護が無かった**。
  「無視されている」を見たら、どのファイルのどの行が効いているかまで見ること
  （`check-ignore -v` は出どころを出す）。
- 上流報告は既存 [#79356](https://github.com/anthropics/claude-code/issues/79356) へコメント済み
  （[投稿](https://github.com/anthropics/claude-code/issues/79356#issuecomment-5157316941)）。
  Linux / Bash / v2.1.220 でも再現することを対照実験ごと足した。
